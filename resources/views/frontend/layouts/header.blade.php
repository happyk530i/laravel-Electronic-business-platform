

 {{--  <div class="flex-center position-ref full-height">
  @if (Route::has('login'))
      <div class="top-right links">
          @auth
              <a href="{{ route('logout') }}">Home</a>
          @else
              <a href="{{ route('login') }}">會員登入</a>

              @if (Route::has('register'))
                  <a href="{{ route('register') }}">註冊新會員</a>
              @endif
          @endauth
      </div>
  @endif  --}}


  
  
  
  <h1 class="site-heading text-center text-white d-none d-lg-block">
  <span class="site-heading-upper text-primary mb-3">日嘗鮮果</span>
  <span class="site-heading-lower">有望水果行</span>
</h1>